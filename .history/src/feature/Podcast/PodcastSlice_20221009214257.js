import { createSlice } from "@reduxjs/toolkit";
const initialState = {
	loadingPodcastList = 
	podcast: []
}

export const PodcastSlice = createSlice({
	name: 'podcast',
	initialState,
	reducer: {

	}
})